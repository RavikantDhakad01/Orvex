const WORKSPACE_ROLE = {
    OWNER: "owner",
    MEMBER: "member"
}

const INVITATION_STATUS = {
    PENDING: "pending",
    ACCEPTED: "accepted",
    REJECTED: "rejected"
}

export { WORKSPACE_ROLE, INVITATION_STATUS }